<UserControl x:Class="DevTools.Presentation.Wpf.Components.DevToolsToolFrame"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="480" d:DesignWidth="800">
    <Grid Background="{StaticResource DevToolsWindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{StaticResource DevToolsSurface}"
                BorderBrush="{StaticResource DevToolsBorder}"
                BorderThickness="0,0,0,1"
                Padding="20,14">
            <DockPanel>
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontSize="{StaticResource DevToolsFontSize.WindowTitle}"
                           Foreground="{StaticResource DevToolsTextPrimary}"
                           FontWeight="SemiBold"
                           DockPanel.Dock="Left"/>
            </DockPanel>
        </Border>

        <!-- Content (single vertical scroll, horizontal disabled) -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Hidden"
                      HorizontalScrollBarVisibility="Disabled"
                      Padding="30,20">
            <ContentPresenter />
        </ScrollViewer>

        <!-- Action Bar -->
        <Border Grid.Row="2"
                Background="{StaticResource DevToolsSurface}"
                BorderBrush="{StaticResource DevToolsBorder}"
                BorderThickness="0,1,0,0"
                Padding="20,10">
            <Grid IsSharedSizeScope="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" SharedSizeGroup="ActionButtonWidth"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="ActionButtonWidth"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding HelpText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="{StaticResource DevToolsTextSecondary}"
                           FontSize="{StaticResource DevToolsFontSize.Label}"
                           VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                        Content="{Binding SecondaryButtonContent, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Command="{Binding SecondaryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Style="{StaticResource DevToolsSecondaryButton}" />

                <Button Grid.Column="3"
                        Content="{Binding PrimaryButtonContent, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Command="{Binding PrimaryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Style="{StaticResource DevToolsPrimaryButton}" />
            </Grid>
        </Border>

        <!-- Status Strip -->
        <Border Grid.Row="3"
                Background="{StaticResource DevToolsStatusIdle}">
            <TextBlock Text="{Binding StatusText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Foreground="{StaticResource DevToolsTextPrimary}"
                       FontSize="{StaticResource DevToolsFontSize.Status}"
                       VerticalAlignment="Center"
                       Margin="10,0"/>
        </Border>
    </Grid>
</UserControl>

