<UserControl x:Class="DevTools.Presentation.Wpf.Components.ProfileSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="400">
    <StackPanel Margin="0,5,0,15">
        <TextBlock Text="Perfil" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,5"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" x:Name="ProfileCombo" 
                      IsEditable="True"
                      SelectionChanged="ProfileCombo_SelectionChanged"
                      MinHeight="32" />
            <Button Grid.Column="1" Content="Salvar" Click="SaveButton_Click" 
                    Style="{DynamicResource SecondaryButtonStyle}" Margin="8,0,0,0"/>
            <Button Grid.Column="2" Content="🗑" Click="DeleteButton_Click" 
                    Style="{DynamicResource IconButtonStyle}" Margin="8,0,0,0" ToolTip="Excluir Perfil"/>
        </Grid>
    </StackPanel>
</UserControl>
