<UserControl x:Class="DevTools.Presentation.Wpf.Components.DevToolsFormSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="{StaticResource DevToolsFontSize.SectionTitle}"
                   Foreground="{StaticResource DevToolsAccent}"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <ContentPresenter Grid.Row="1"
                          Margin="0,0,0,{StaticResource DevToolsSpacing.FieldGroup}"/>
    </Grid>
</UserControl>

