<Window x:Class="DevTools.Presentation.Wpf.Views.NgrokWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:DevTools.Presentation.Wpf.Components"
        mc:Ignorable="d"
        Title="DevTools - Ngrok"
        Height="550" Width="850"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        AllowsTransparency="True"
        Background="{StaticResource DevToolsWindowBackground}"
        BorderBrush="{StaticResource DevToolsBorder}"
        BorderThickness="1">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" ResizeBorderThickness="5" />
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="Transparent" 
                MouseLeftButtonDown="Header_MouseLeftButtonDown">
            <DockPanel Margin="10,0">
                <Button Content="X" 
                        Width="28"
                        Height="24"
                        DockPanel.Dock="Right"
                        Margin="8,8,0,8"
                        Click="Cancel_Click"
                        Style="{StaticResource DevToolsSecondaryButton}"/>
                
                <TextBlock Text="Ngrok Manager" 
                           VerticalAlignment="Center" 
                           FontSize="{StaticResource DevToolsFontSize.SectionTitle}"
                           Foreground="{StaticResource DevToolsTextPrimary}"
                           FontWeight="SemiBold"/>
            </DockPanel>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Parameters -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,16,0">
                <StackPanel>
                    <StackPanel>
                        <TextBlock Text="Ação" FontSize="{StaticResource DevToolsFontSize.SectionTitle}" Foreground="{StaticResource DevToolsAccent}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <ComboBox x:Name="ActionCombo" Style="{StaticResource DevToolsComboBox}" SelectionChanged="ActionCombo_SelectionChanged" Margin="0,0,0,16">
                            <ComboBoxItem Content="Listar Túneis (ListTunnels)" Tag="ListTunnels" IsSelected="True"/>
                            <ComboBoxItem Content="Iniciar HTTP (StartHttp)" Tag="StartHttp"/>
                            <ComboBoxItem Content="Fechar Túnel (CloseTunnel)" Tag="CloseTunnel"/>
                            <ComboBoxItem Content="Matar Processos (KillAll)" Tag="KillAll"/>
                            <ComboBoxItem Content="Status do Serviço (Status)" Tag="Status"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="0,16,0,0">
                        <TextBlock Text="Configuração do Túnel" FontSize="{StaticResource DevToolsFontSize.SectionTitle}" Foreground="{StaticResource DevToolsAccent}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <StackPanel Margin="0,0,0,16">
                            <Label Content="Porta Local" Foreground="{StaticResource DevToolsTextSecondary}"/>
                            <TextBox x:Name="PortBox" Style="{StaticResource DevToolsTextInput}" Text="8080"/>
                            
                            <Label Content="Subdomínio (Opcional)" Foreground="{StaticResource DevToolsTextSecondary}"/>
                            <TextBox x:Name="SubdomainBox" Style="{StaticResource DevToolsTextInput}"/>
                            
                            <Label Content="Nome do Túnel (para fechar)" Foreground="{StaticResource DevToolsTextSecondary}"/>
                            <TextBox x:Name="TunnelNameBox" Style="{StaticResource DevToolsTextInput}"/>
                            
                            <Label Content="Auth Token (Opcional)" Foreground="{StaticResource DevToolsTextSecondary}"/>
                            <TextBox x:Name="AuthTokenBox" Style="{StaticResource DevToolsTextInput}"/>
                        </StackPanel>
                    </StackPanel>

                    <Button x:Name="RunButton" Content="Executar" Click="Run_Click" 
                            Margin="0,24,0,0" Height="40" 
                            Style="{StaticResource DevToolsPrimaryButton}"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Results -->
            <Border Grid.Column="1" Background="{StaticResource DevToolsSurface}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Resultado" FontWeight="Bold" Margin="0,0,0,12" Foreground="{StaticResource DevToolsTextPrimary}"/>
                    
                    <c:RunSummaryControl x:Name="RunSummary" Grid.Row="1"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
