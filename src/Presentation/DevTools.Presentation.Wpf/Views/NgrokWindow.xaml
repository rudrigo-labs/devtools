<Window x:Class="DevTools.Presentation.Wpf.Views.NgrokWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:DevTools.Presentation.Wpf.Components"
        mc:Ignorable="d"
        Title="Ngrok Controller" Height="640" Width="900" WindowStartupLocation="CenterScreen"
        Style="{StaticResource ModernWindowStyle}">

    <Window.Resources>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <c:DevToolsToolFrame x:Name="MainFrame"
                         Title="Expor Porta (Ngrok)"
                         HelpText="Cria túnel http público para uma porta local."
                         StatusText="⚫ Parado"
                         PrimaryButtonContent="Expor Porta"
                         SecondaryButtonContent="Cancelar"
                         PrimaryButtonClick="StartButton_Click"
                         SecondaryButtonClick="CloseButton_Click"
                         CloseButtonClick="CloseButton_Click">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- New Tunnel Form -->
            <Border Grid.Row="0" Background="#252526" Padding="20" Margin="0,0,0,10" CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="Porta Local (ex: 5000)" Margin="0,0,0,5" Foreground="{StaticResource PrimaryTextBrush}"/>
                    <TextBox x:Name="PortInput" Text="5000" PreviewTextInput="NumberValidationTextBox" Style="{StaticResource ModernTextBoxStyle}"/>
                </StackPanel>
            </Border>

            <!-- Tunnels List -->
            <Grid Grid.Row="1" Margin="20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="Túneis Ativos" FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                    <Button Content="Atualizar" Click="RefreshButton_Click" HorizontalAlignment="Right" Style="{StaticResource SecondaryButtonStyle}" Padding="10,5" FontSize="12"/>
                </Grid>

                <ListBox x:Name="TunnelsList" Grid.Row="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#333333" CornerRadius="4" Margin="0,0,0,5" Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel>
                                        <TextBlock Text="{Binding PublicUrl}" FontWeight="SemiBold" Foreground="#4EC9B0" FontSize="14"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                            <TextBlock Text="➡" Margin="0,0,5,0" Foreground="Gray"/>
                                            <TextBlock Text="{Binding Config.Addr}" Foreground="#CCCCCC"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <Button Grid.Column="1" Content="Copiar" Click="CopyUrl_Click" Tag="{Binding PublicUrl}" Style="{StaticResource SecondaryButtonStyle}" Margin="10,0,0,0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                
                <TextBlock x:Name="EmptyStateText" Grid.Row="1" Text="Nenhum túnel ativo." HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#555555" Visibility="Collapsed"/>
            </Grid>

            <!-- Footer Actions -->
            <StackPanel Grid.Row="2" Margin="20,10,20,0" Orientation="Horizontal">
                <Button Content="Matar Processos Ngrok" Click="KillAll_Click" Style="{StaticResource DangerButtonStyle}" Padding="15,8"/>
            </StackPanel>
        </Grid>
    </c:DevToolsToolFrame>
</Window>
