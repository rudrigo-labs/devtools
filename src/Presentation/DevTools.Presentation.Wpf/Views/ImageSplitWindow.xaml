<Window x:Class="DevTools.Presentation.Wpf.Views.ImageSplitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:DevTools.Presentation.Wpf.Components"
        mc:Ignorable="d"
        Title="Dividir Imagens" Height="640" Width="900" WindowStartupLocation="CenterOwner"
        Style="{StaticResource ModernWindowStyle}"
        ShowInTaskbar="False"
        Topmost="True">

    <Window.Resources>
        <!-- Styles removed to use global theme -->
    </Window.Resources>

    <c:DevToolsToolFrame Title="Dividir Imagens"
                         HelpText="Divide imagens em grade configurável."
                         PrimaryButtonContent="Processar"
                         SecondaryButtonContent="Cancelar"
                         PrimaryButtonClick="ProcessButton_Click"
                         SecondaryButtonClick="CloseButton_Click"
                         CloseButtonClick="CloseButton_Click">
        
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,5,0">
        <StackPanel Margin="0,10,0,0">
            
            <!-- Input File -->
                <c:PathSelector x:Name="InputPathSelector" Title="Imagem de Entrada" IsFolderPicker="False" Margin="0,0,0,15"/>

                <!-- Output Directory -->
                <c:PathSelector x:Name="OutputPathSelector" Title="Pasta de Saída (Opcional - Padrão: Mesma da Entrada)" IsFolderPicker="True" />

                <!-- Settings -->
                <Grid Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Alpha Threshold -->
                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                        <Label Content="Limiar Alpha (0-255):"/>
                        <TextBox x:Name="AlphaBox" Text="10" PreviewTextInput="NumberValidationTextBox" Style="{StaticResource ModernTextBoxStyle}"/>
                        <TextBlock Text="Transparência para considerar fundo." 
                                   FontSize="10" Foreground="#888888" Margin="0,2,0,0"/>
                    </StackPanel>

                    <!-- Min Region Size -->
                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                        <Label Content="Tamanho Mínimo (Px):"/>
                        <TextBox x:Name="MinSizeBox" Text="3" PreviewTextInput="NumberValidationTextBox" Style="{StaticResource ModernTextBoxStyle}"/>
                        <TextBlock Text="Largura/Altura mínima para recortar." 
                                   FontSize="10" Foreground="#888888" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>

                <CheckBox x:Name="OverwriteCheck" Content="Sobrescrever arquivos existentes" 
                          Foreground="#DDDDDD" Margin="0,15,0,0" VerticalContentAlignment="Center"/>

            </StackPanel>
        </ScrollViewer>
    </c:DevToolsToolFrame>
</Window>
