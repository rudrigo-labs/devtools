<Window x:Class="DevTools.Presentation.Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:c="clr-namespace:DevTools.Presentation.Wpf.Components"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="DevTools Hub" Height="840" Width="1280"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary>
            <!-- Recursos locais para esta janela -->
            <SolidColorBrush x:Key="SidebarBackground" Color="#252526"/>
            <SolidColorBrush x:Key="ContentBackground" Color="#1E1E1E"/>
            <SolidColorBrush x:Key="AccentColor" Color="#007ACC"/>
            
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            
            <Style TargetType="Button" x:Key="SidebarButtonStyle">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#CCCCCC"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="Padding" Value="15,0,0,0"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}">
                                <ContentPresenter VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#3E3E42"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <materialDesign:DialogHost x:Name="RootDialog" Identifier="RootDialog" DialogTheme="Dark">
        <materialDesign:DialogHost.DialogContent>
            <Border Padding="20" Background="#2D2D30" CornerRadius="8" BorderBrush="#3F3F46" BorderThickness="1">
                <StackPanel Width="320">
                    <TextBlock Text="Encerrar Aplicação" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,8"/>
                    <TextBlock Text="Deseja realmente sair do DevTools?" Foreground="#CCCCCC" Margin="0,0,0,18"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <!-- BOTÃO NÃO (fecha o diálogo) -->
                        <Button Content="NÃO"
                            Click="DialogNoButton_Click"
                            Margin="0,0,10,0"
                            Padding="14,6"/>

                        <!-- BOTÃO SIM (encerra de verdade) -->
                        <Button Content="SIM"
                            Click="Shutdown_Click"
                            Background="#E81123"
                            Foreground="White"
                            Padding="14,6"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </materialDesign:DialogHost.DialogContent>

        <Border Background="{DynamicResource ContentBackground}" 
            BorderBrush="{DynamicResource DevToolsBrushBorder}" 
            BorderThickness="1" 
            CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0" Background="{DynamicResource SidebarBackground}" CornerRadius="8,0,0,8">
                    <StackPanel Margin="0,20,0,0">
                        <StackPanel Orientation="Horizontal" Margin="20,0,0,30">
                            <TextBlock Text="&#xE80F;" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="{DynamicResource AccentColor}" Margin="0,2,10,0"/>
                            <TextBlock Text="DevTools" FontSize="18" FontWeight="Bold" Foreground="White"/>
                        </StackPanel>

                        <Button Content="Ferramentas" Style="{DynamicResource SidebarButtonStyle}" Tag="Tools" Click="NavButton_Click" Margin="0,0,0,5"/>
                        <Button Content="Execuções (Jobs)" Style="{DynamicResource SidebarButtonStyle}" Tag="Jobs" Click="NavButton_Click" Margin="0,0,0,5"/>
                        <Button Content="Configurações" Style="{DynamicResource SidebarButtonStyle}" Tag="Settings" Click="NavButton_Click" Margin="0,0,0,20"/>

                        <Button Content="Minimizar para Bandeja" Style="{DynamicResource SidebarButtonStyle}" Click="MinimizeToTray_Click" Margin="0,0,0,5"/>
                        <Button Content="Encerrar"
                            Style="{DynamicResource SidebarButtonStyle}"
                            Foreground="#E81123"
                            Command="{x:Static materialDesign:DialogHost.OpenDialogCommand}"
                            CommandTarget="{Binding ElementName=RootDialog}"/>
                    </StackPanel>
                </Border>

                <!-- Main Content -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <!-- Increased Header Height -->
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Window Drag/Close -->
                    <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="Window_MouseLeftButtonDown">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Click="MinimizeButton_Click"
                                Content="&#xE921;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                Width="40" Height="40" Background="Transparent" Foreground="#999999"
                                BorderThickness="0">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#3E3E42"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <Button Click="CloseButton_Click"
                                Content="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                Width="40" Height="40" Background="Transparent" Foreground="#999999"
                                BorderThickness="0">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#C42B1C"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <!-- Dynamic Content Area -->
                    <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="30,0,30,30" x:Name="MainTabControl">
                        <TabControl.Resources>
                            <Style TargetType="TabItem">
                                <!-- Removemos a visibilidade Collapsed aqui para garantir que os cards apareçam se selecionado -->
                            </Style>
                        </TabControl.Resources>

                        <!-- Tab: Tools -->
                        <TabItem x:Name="TabTools">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,15,0">
                                <StackPanel>
                                    <TextBlock Text="Ferramentas Disponíveis" FontSize="24" FontWeight="Light" Foreground="White" Margin="0,0,0,20"/>

                                    <WrapPanel Orientation="Horizontal" Margin="0,0,-15,0">
                                        <!-- Cards de Ferramentas -->
                                        <Button Style="{DynamicResource ToolCardStyle}" Content="Notas Rápidas" 
                                            Tag="{DynamicResource Icon.Notes}" ToolTip="Anotações flutuantes always-on-top" Click="OpenTool_Click" CommandParameter="Notes"/>

                                        <Button Style="{DynamicResource ToolCardStyle}" Content="Organizador de Arquivos" 
                                            Tag="{DynamicResource Icon.Organizer}" ToolTip="Move/organiza por extensão/data conforme categorias" Click="OpenTool_Click" CommandParameter="Organizer"/>

                                        <Button Style="{DynamicResource ToolCardStyle}" Content="Renomear Projeto .NET" 
                                            Tag="{DynamicResource Icon.Rename}" ToolTip="Renomeia projeto/solution e namespaces C# com backup" Click="OpenTool_Click" CommandParameter="Rename"/>

                                        <Button Style="{DynamicResource ToolCardStyle}" Content="Snapshot do Projeto" 
                                            Tag="{DynamicResource Icon.Snapshot}" ToolTip="Gera ZIP do projeto com filtros" Click="OpenTool_Click" CommandParameter="Snapshot"/>

                                        <Button Style="{DynamicResource ToolCardStyle}" Content="Dividir Imagens" 
                                            Tag="{DynamicResource Icon.ImageSplitter}" ToolTip="Divide imagens em grade configurável" Click="OpenTool_Click" CommandParameter="ImageSplitter"/>

                                        <Button Style="{DynamicResource ToolCardStyle}" Content="Converter para UTF‑8" 
                                            Tag="{DynamicResource Icon.Utf8Convert}" ToolTip="Converte arquivos para UTF‑8 preservando conteúdo" Click="OpenTool_Click" CommandParameter="Utf8Convert"/>

                                        <Button Style="{DynamicResource ToolCardStyle}" Content="Buscar Texto em Arquivos" 
                                            Tag="{DynamicResource Icon.SearchText}" ToolTip="Busca rápida com regex e filtros" Click="OpenTool_Click" CommandParameter="SearchText"/>

                                        <Button Style="{DynamicResource ToolCardStyle}" Content="EF Core Migrations" 
                                            Tag="{DynamicResource Icon.Migrations}" ToolTip="Cria/aplica migrations em contextos EF Core" Click="OpenTool_Click" CommandParameter="Migrations"/>

                                        <Button Style="{DynamicResource ToolCardStyle}" Content="Coletor de Arquivos" 
                                            Tag="{DynamicResource Icon.Harvest}" ToolTip="Copia arquivos filtrados para pasta de saída" Click="OpenTool_Click" CommandParameter="Harvest"/>

                                        <Button Style="{DynamicResource ToolCardStyle}" Content="Túnel SSH" 
                                            Tag="{DynamicResource Icon.SSHTunnel}" ToolTip="Gerenciador de túneis SSH" Click="OpenTool_Click" CommandParameter="SSHTunnel"/>

                                    </WrapPanel>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>

                        <!-- Tab: Jobs -->
                        <TabItem x:Name="TabJobs">
                            <DockPanel LastChildFill="True">
                                <TextBlock DockPanel.Dock="Top" Text="Centro de Execuções" FontSize="24" FontWeight="Light" Foreground="White" Margin="0,0,0,20"/>

                                <!-- Detalhes / Logs (Bottom) -->
                                <Expander DockPanel.Dock="Bottom" Header="Detalhes / Logs" Margin="0,10,0,0" IsExpanded="True" BorderBrush="{DynamicResource DevToolsBrushBorder}">
                                    <TextBox Text="{Binding SelectedItem.Logs, ElementName=JobsDataGrid, Mode=OneWay}" 
                                         Height="150" 
                                         Background="#1E1E1E" 
                                         Foreground="#D4D4D4"
                                         FontFamily="Consolas"
                                         IsReadOnly="True"
                                         VerticalScrollBarVisibility="Auto"
                                         BorderThickness="0"
                                         Padding="5"/>
                                </Expander>

                                <!-- Lista de Jobs (Fill) -->
                                <DataGrid x:Name="JobsDataGrid" 
                                      AutoGenerateColumns="False" 
                                      IsReadOnly="True" 
                                      Background="Transparent"
                                      BorderThickness="0"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="Horizontal"
                                      RowBackground="Transparent"
                                      Foreground="#CCCCCC">
                                    <DataGrid.Resources>
                                        <Style TargetType="DataGridColumnHeader">
                                            <Setter Property="Background" Value="#2D2D30"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="Padding" Value="10,5"/>
                                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                            <Setter Property="BorderBrush" Value="#3F3F46"/>
                                        </Style>
                                        <Style TargetType="DataGridCell">
                                            <Setter Property="Padding" Value="10"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="DataGridCell">
                                                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                                            <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter Property="Background" Value="#3E3E42"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGrid.Resources>

                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Tarefa" Binding="{Binding Name}" Width="2*"/>
                                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                                        <DataGridTemplateColumn Header="Progresso" Width="1.5*">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Grid>
                                                        <ProgressBar Value="{Binding ProgressPercent}" Maximum="100" Height="4" VerticalAlignment="Center" Background="#333333" BorderThickness="0" Foreground="{DynamicResource AccentColor}"/>
                                                        <TextBlock Text="{Binding ProgressPercent, StringFormat={}{0}%}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" Foreground="White" Margin="0,-15,0,0"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header="Mensagem" Binding="{Binding Message}" Width="3*"/>
                                        <DataGridTextColumn Header="Início" Binding="{Binding StartedAt, StringFormat=HH:mm:ss}" Width="80"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </DockPanel>
                        </TabItem>

                        <!-- Tab: Settings -->
                        <TabItem x:Name="TabSettings">
                            <Grid>
                                <!-- Lista de Configurações (Cards) -->
                                <ScrollViewer x:Name="SettingsListPanel" VerticalScrollBarVisibility="Auto">
                                    <StackPanel Margin="0,10">
                                        <TextBlock Text="Configurações do Projeto" FontSize="24" FontWeight="Light" Foreground="White" Margin="0,0,0,20"/>

                                        <!-- Seção: Configurações Gerais -->
                                        <TextBlock Text="Configurações Gerais" FontSize="18" Foreground="{DynamicResource AccentColor}" Margin="0,0,0,10"/>
                                        <WrapPanel Orientation="Horizontal" Margin="0,0,-15,20">
    

                                            <Button Style="{DynamicResource ToolCardStyle}" Tag="{DynamicResource Icon.Harvest}" Content="Harvest" 
                                                ToolTip="Configurar regras de extração e IA" Width="200" Click="OpenHarvestSettings_Click"/>

                                            <Button Style="{DynamicResource ToolCardStyle}" Tag="{DynamicResource Icon.Organizer}" Content="Organizer" 
                                                ToolTip="Categorias e regras de organização" Width="200" Click="OpenOrganizerSettings_Click"/>

                                            <Button Style="{DynamicResource ToolCardStyle}" Tag="{DynamicResource Icon.Migrations}" Content="Migrations" 
                                                ToolTip="Caminhos e contextos do EF Core" Width="200" Click="OpenMigrationsSettings_Click"/>

                                            <Button Style="{DynamicResource ToolCardStyle}" Tag="{DynamicResource Icon.Ngrok}" Content="Ngrok" 
                                                ToolTip="Caminho do executável e token" Width="200" Click="OpenNgrokSettings_Click"/>
                                        </WrapPanel>

                                        <!-- Seção: Perfis de Ferramentas -->
                                        <TextBlock Text="Perfis de Ferramentas" FontSize="18" Foreground="{DynamicResource AccentColor}" Margin="0,20,0,10"/>
                                        <WrapPanel Orientation="Horizontal" Margin="0,0,-15,0">
                                            <Button Style="{DynamicResource ToolCardStyle}" Tag="{DynamicResource Icon.Rename}" Content="Renomeação" 
                                                ToolTip="Perfis de renomeação de projeto" Width="200" Click="OpenToolProfiles_Click" CommandParameter="Rename"/>

                                            <Button Style="{DynamicResource ToolCardStyle}" Tag="{DynamicResource Icon.Migrations}" Content="EF Migrations" 
                                                ToolTip="Perfis de execução de migrations" Width="200" Click="OpenToolProfiles_Click" CommandParameter="Migrations"/>

                                            <Button Style="{DynamicResource ToolCardStyle}" Tag="{DynamicResource Icon.Harvest}" Content="Harvest" 
                                                ToolTip="Perfis de coleta de arquivos" Width="200" Click="OpenToolProfiles_Click" CommandParameter="Harvest"/>
                                                
                                            <Button Style="{DynamicResource ToolCardStyle}" Tag="{DynamicResource Icon.SearchText}" Content="Busca de Texto" 
                                                ToolTip="Perfis de busca de texto em arquivos" Width="200" Click="OpenToolProfiles_Click" CommandParameter="SearchText"/>

                                            <Button Style="{DynamicResource ToolCardStyle}" Tag="{DynamicResource Icon.Snapshot}" Content="Snapshot" 
                                                ToolTip="Perfis de snapshot de projeto" Width="200" Click="OpenToolProfiles_Click" CommandParameter="Snapshot"/>

                                            <Button Style="{DynamicResource ToolCardStyle}" Tag="{DynamicResource Icon.SSHTunnel}" Content="Túnel SSH" 
                                                ToolTip="Perfis de conexão e túnel SSH" Width="200" Click="OpenToolProfiles_Click" CommandParameter="SSHTunnel"/>
                                        </WrapPanel>
                                    </StackPanel>
                                </ScrollViewer>

                                <!-- Painel: Gestão de Perfis de Ferramenta -->
                                <Grid x:Name="ToolProfilesSettingsPanel" Visibility="Collapsed">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <!-- Header com Voltar -->
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                        <Button Content="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="16" 
                                            Foreground="White" Background="Transparent" BorderThickness="0"
                                            Click="BackToSettingsList_Click" Margin="0,0,10,0"/>
                                        <TextBlock x:Name="ToolProfilesTitle" Text="Perfis: [Ferramenta]" FontSize="20" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <!-- Conteúdo Gestão de Perfis -->
                                    <Grid Grid.Row="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="250"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Lista de Perfis -->
                                        <Border BorderBrush="{DynamicResource DevToolsBrushBorder}" BorderThickness="0" Padding="0,0,10,0">
                                            <DockPanel>
                                                <Button DockPanel.Dock="Bottom" Content="+ Novo Perfil" Style="{DynamicResource SecondaryButtonStyle}" 
                                                    Height="35" Margin="0,10,0,0" Click="AddToolProfile_Click"/>
                                                <ListBox x:Name="ToolProfilesList" Background="Transparent" BorderThickness="0" 
                                                     Foreground="White" SelectionChanged="ToolProfile_SelectionChanged">
                                                    <ListBox.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Margin="0,2">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                                                <TextBlock Grid.Column="1" Text="⭐" Foreground="Gold" 
                                                                           Visibility="{Binding IsDefault, Converter={StaticResource BooleanToVisibilityConverter}}" 
                                                                           ToolTip="Perfil Padrão" Margin="5,0,0,0"/>
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ListBox.ItemTemplate>
                                                    <ListBox.ItemContainerStyle>
                                                        <Style TargetType="ListBoxItem">
                                                            <Setter Property="Padding" Value="10,8"/>
                                                            <Setter Property="Margin" Value="0,0,0,2"/>
                                                            <Setter Property="Background" Value="#2D2D30"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="ListBoxItem">
                                                                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                            <ContentPresenter/>
                                                                        </Border>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsSelected" Value="True">
                                                                                <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
                                                                            </Trigger>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Cursor" Value="Hand"/>
                                                                                <Setter Property="Background" Value="#3E3E42"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </ListBox.ItemContainerStyle>
                                                </ListBox>
                                            </DockPanel>
                                        </Border>

                                                <!-- Editor de Perfil -->
                                        <ScrollViewer Grid.Column="1" Margin="20,0,0,0" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
                                            <StackPanel x:Name="ToolProfileEditForm" Visibility="Collapsed">
                                                
                                                <materialDesign:Card Padding="20" Margin="0,0,0,24" Background="#2D2D30">
                                                    <StackPanel>
                                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                                            <materialDesign:PackIcon Kind="Settings" Foreground="{DynamicResource AccentColor}" VerticalAlignment="Center" Margin="0,0,10,0" Width="20" Height="20"/>
                                                            <TextBlock Text="Configurações do Perfil" FontSize="16" Foreground="White" FontWeight="SemiBold"/>
                                                        </StackPanel>

                                                        <StackPanel>
                                                            <Label Content="Nome do Perfil" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,6"/>
                                                            <TextBox x:Name="ToolProfileNameInput" Foreground="White"/>
                                                        </StackPanel>

                                                        <!-- Container para campos específicos injetados -->
                                                        <StackPanel x:Name="ToolProfileFieldsContainer" Margin="0,20,0,0"/>

                                                        <CheckBox x:Name="ToolProfileIsDefaultCheck" 
                                                              Content="Usar como perfil padrão" 
                                                              Margin="0,24,0,0" 
                                                              Foreground="White"
                                                              Cursor="Hand"/>
                                                    </StackPanel>
                                                </materialDesign:Card>

                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,20">
                                                    <Button Content="Remover" Click="DeleteToolProfile_Click" 
                                                            Style="{DynamicResource SecondaryButtonStyle}" Width="120" Foreground="#E81123" Margin="0,0,10,0"/>
                                                    <Button Content="Cancelar" Click="BackToSettingsList_Click" 
                                                            Style="{DynamicResource SecondaryButtonStyle}" Width="120" Margin="0,0,10,0"/>
                                                    <Button Content="Salvar" Click="SaveToolProfile_Click" 
                                                            Style="{DynamicResource PrimaryButtonStyle}" Width="120"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </ScrollViewer>
                                    </Grid>
                                </Grid>



                                <!-- Painel: Configuração Harvest -->
                                <Grid x:Name="HarvestSettingsPanel" Visibility="Collapsed">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                        <Button Content="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="16" 
                                            Foreground="White" Background="Transparent" BorderThickness="0"
                                            Click="BackToSettingsList_Click" Margin="0,0,10,0"/>
                                        <TextBlock Text="Configuração: Harvest" FontSize="20" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                        <StackPanel Margin="0,0,20,0">
                                            
                                            <!-- Grupo: Regras de Filtragem -->
                                            <materialDesign:Card Padding="20" Margin="0,0,0,24" Background="#2D2D30">
                                                <StackPanel>
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                                        <materialDesign:PackIcon Kind="FilterVariant" Foreground="{DynamicResource AccentColor}" VerticalAlignment="Center" Margin="0,0,10,0" Width="20" Height="20"/>
                                                        <TextBlock Text="Regras de Filtragem" FontSize="16" Foreground="White" FontWeight="SemiBold"/>
                                                    </StackPanel>

                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="20"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>

                                                        <StackPanel>
                                                            <Label Content="Extensões Permitidas (ex: .cs, .ts)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,6"/>
                                                            <TextBox x:Name="HarvestExtensions" 
                                                                 Foreground="White" Height="80" AcceptsReturn="True" TextWrapping="Wrap"/>
                                                        </StackPanel>

                                                        <StackPanel Grid.Column="2">
                                                            <Label Content="Pastas Ignoradas (ex: bin, obj)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,6"/>
                                                            <TextBox x:Name="HarvestExcludeDirs" 
                                                                 Foreground="White" Height="80" AcceptsReturn="True" TextWrapping="Wrap"/>
                                                        </StackPanel>
                                                    </Grid>

                                                    <StackPanel Margin="0,20,0,0" Width="200" HorizontalAlignment="Left">
                                                        <Label Content="Tamanho Máximo de Arquivo (KB)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,6"/>
                                                        <TextBox x:Name="HarvestMaxFileSizeInput" Foreground="White"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </materialDesign:Card>

                                            <!-- Grupo: Limites e Top -->
                                            <materialDesign:Card Padding="20" Margin="0,0,0,24" Background="#2D2D30">
                                                <StackPanel>
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                                        <materialDesign:PackIcon Kind="ChartLine" Foreground="{DynamicResource AccentColor}" VerticalAlignment="Center" Margin="0,0,10,0" Width="20" Height="20"/>
                                                        <TextBlock Text="Limites Padrão" FontSize="16" Foreground="White" FontWeight="SemiBold"/>
                                                    </StackPanel>

                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="20"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        
                                                        <StackPanel>
                                                            <Label Content="Score Mínimo (0-100)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,6"/>
                                                            <TextBox x:Name="HarvestMinScore" Foreground="White"/>
                                                        </StackPanel>
                                                        
                                                        <StackPanel Grid.Column="2">
                                                            <Label Content="Top Arquivos (Quantidade)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,6"/>
                                                            <TextBox x:Name="HarvestTopDefault" Foreground="White"/>
                                                        </StackPanel>
                                                    </Grid>
                                                </StackPanel>
                                            </materialDesign:Card>

                                            <!-- Grupo: Pesos de IA -->
                                            <materialDesign:Card Padding="20" Margin="0,0,0,24" Background="#2D2D30">
                                                <StackPanel>
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                                        <materialDesign:PackIcon Kind="Robot" Foreground="{DynamicResource AccentColor}" VerticalAlignment="Center" Margin="0,0,10,0" Width="20" Height="20"/>
                                                        <TextBlock Text="Pesos de Relevância (IA)" FontSize="16" Foreground="White" FontWeight="SemiBold"/>
                                                    </StackPanel>

                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="20"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>

                                                        <StackPanel>
                                                            <Label Content="Fan-In (Entradas)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,6"/>
                                                            <TextBox x:Name="HarvestWeightFanIn" Foreground="White"/>

                                                            <Label Content="Densidade de Keywords" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,20,0,6"/>
                                                            <TextBox x:Name="HarvestWeightDensity" Foreground="White"/>
                                                        </StackPanel>

                                                        <StackPanel Grid.Column="2">
                                                            <Label Content="Fan-Out (Saídas)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,6"/>
                                                            <TextBox x:Name="HarvestWeightFanOut" Foreground="White"/>

                                                            <Label Content="Penalidade Dead Code" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,20,0,6"/>
                                                            <TextBox x:Name="HarvestWeightDeadCode" Foreground="White"/>
                                                        </StackPanel>
                                                    </Grid>
                                                </StackPanel>
                                            </materialDesign:Card>

                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,20">
                                                <Button Content="Cancelar" Click="BackToSettingsList_Click" 
                                                        Style="{DynamicResource SecondaryButtonStyle}" Width="120" Margin="0,0,10,0"/>
                                                <Button Content="Salvar" Click="SaveHarvestSettings_Click" 
                                                        Style="{DynamicResource PrimaryButtonStyle}" Width="120"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </ScrollViewer>
                                </Grid>

                                <!-- Painel: Configuração Organizer -->
                                <Grid x:Name="OrganizerSettingsPanel" Visibility="Collapsed">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                        <Button Content="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="16" 
                                            Foreground="White" Background="Transparent" BorderThickness="0"
                                            Click="BackToSettingsList_Click" Margin="0,0,10,0"/>
                                        <TextBlock Text="Configuração: Organizer" FontSize="20" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <Grid Grid.Row="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="250"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Lista de Categorias -->
                                        <Border BorderBrush="{DynamicResource DevToolsBrushBorder}" BorderThickness="0" Padding="0,0,10,0">
                                            <DockPanel>
                                                <Button DockPanel.Dock="Bottom" Content="+ Nova Categoria" Style="{DynamicResource SecondaryButtonStyle}" 
                                                    Height="35" Margin="0,10,0,0" Click="AddOrganizerCategory_Click"/>
                                                <ListBox x:Name="OrganizerCategoriesList" Background="Transparent" BorderThickness="0" 
                                                     Foreground="White" DisplayMemberPath="Name" SelectionChanged="OrganizerCategory_SelectionChanged">
                                                    <ListBox.ItemContainerStyle>
                                                        <Style TargetType="ListBoxItem">
                                                            <Setter Property="Padding" Value="10,8"/>
                                                            <Setter Property="Margin" Value="0,0,0,2"/>
                                                            <Setter Property="Background" Value="#2D2D30"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="ListBoxItem">
                                                                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                            <ContentPresenter/>
                                                                        </Border>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsSelected" Value="True">
                                                                                <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
                                                                            </Trigger>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Cursor" Value="Hand"/>
                                                                                <Setter Property="Background" Value="#3E3E42"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </ListBox.ItemContainerStyle>
                                                </ListBox>
                                            </DockPanel>
                                        </Border>

                                        <!-- Form Organizer -->
                                        <ScrollViewer Grid.Column="1" Margin="20,0,0,0" VerticalScrollBarVisibility="Auto">
                                            <StackPanel x:Name="OrganizerEditForm" Visibility="Collapsed">
                                                
                                                <!-- Grupo: Detalhes da Categoria -->
                                                <materialDesign:Card Padding="16" Margin="0,0,0,16" Background="#2D2D30">
                                                    <StackPanel>
                                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                                            <materialDesign:PackIcon Kind="Category" Foreground="{DynamicResource AccentColor}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                            <TextBlock Text="Detalhes da Categoria" FontSize="16" Foreground="White" FontWeight="SemiBold"/>
                                                        </StackPanel>

                                                        <StackPanel>
                                                            <Label Content="Nome da Categoria" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,2"/>
                                                            <TextBox x:Name="OrgCatName" Foreground="White"/>
                                                        </StackPanel>

                                                        <StackPanel Margin="0,16,0,0">
                                                            <Label Content="Nome da Pasta Destino" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,2"/>
                                                            <TextBox x:Name="OrgCatFolder" Foreground="White"/>
                                                        </StackPanel>

                                                        <StackPanel Margin="0,16,0,0">
                                                            <Label Content="Palavras-Chave (separadas por vírgula)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,2"/>
                                                            <TextBox x:Name="OrgCatKeywords" Foreground="White" Height="80" AcceptsReturn="True" TextWrapping="Wrap"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                </materialDesign:Card>

                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,20">
                                                    <Button Content="Remover" Click="DeleteOrganizerCategory_Click" 
                                                            Style="{DynamicResource SecondaryButtonStyle}" Width="120" Foreground="#E81123" Margin="0,0,10,0"/>
                                                    <Button Content="Cancelar" Click="BackToSettingsList_Click" 
                                                            Style="{DynamicResource SecondaryButtonStyle}" Width="120" Margin="0,0,10,0"/>
                                                    <Button Content="Salvar" Click="SaveOrganizerCategory_Click" 
                                                            Style="{DynamicResource PrimaryButtonStyle}" Width="120"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </ScrollViewer>
                                    </Grid>
                                </Grid>

                                <!-- Painel: Configuração Migrations -->
                                <Grid x:Name="MigrationsSettingsPanel" Visibility="Collapsed">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                        <Button Content="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="16" 
                                            Foreground="White" Background="Transparent" BorderThickness="0"
                                            Click="BackToSettingsList_Click" Margin="0,0,10,0"/>
                                        <TextBlock Text="Configuração: EF Core Migrations" FontSize="20" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                        <StackPanel Margin="0,0,20,0">
                                            
                                            <!-- Grupo: Caminhos do Projeto -->
                                            <materialDesign:Card Padding="16" Margin="0,0,0,16" Background="#2D2D30">
                                                <StackPanel>
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                                    <materialDesign:PackIcon Kind="Folder" Foreground="{DynamicResource AccentColor}" VerticalAlignment="Center" Margin="0,0,10,0" Width="20" Height="20"/>
                                                    <TextBlock Text="Caminhos do Projeto" FontSize="16" Foreground="White" FontWeight="SemiBold"/>
                                                </StackPanel>

                                                <StackPanel Margin="0,0,0,20">
                                                    <c:PathSelector x:Name="MigRootPathSelector" Title="Caminho Raiz do Projeto (Root Path)" IsFolderPicker="True"/>
                                                </StackPanel>

                                                <StackPanel>
                                                    <c:PathSelector x:Name="MigStartupPathSelector" Title="Caminho do Projeto de Startup (Startup Project)" IsFolderPicker="True"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </materialDesign:Card>

                                        <!-- Grupo: Configuração do Contexto -->
                                        <materialDesign:Card Padding="20" Margin="0,0,0,24" Background="#2D2D30">
                                            <StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                                    <materialDesign:PackIcon Kind="DatabaseSettings" Foreground="{DynamicResource AccentColor}" VerticalAlignment="Center" Margin="0,0,10,0" Width="20" Height="20"/>
                                                    <TextBlock Text="Configuração do Contexto" FontSize="16" Foreground="White" FontWeight="SemiBold"/>
                                                </StackPanel>

                                                <StackPanel>
                                                    <Label Content="Nome Completo do DbContext (ex: MyApp.Data.AppDbContext)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,6"/>
                                                    <TextBox x:Name="MigContextInput" Foreground="White" FontFamily="Consolas"/>
                                                </StackPanel>

                                                <StackPanel Margin="0,20,0,0">
                                                    <Label Content="Argumentos Adicionais (dotnet ef ...)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,6"/>
                                                    <TextBox x:Name="MigArgsInput" Foreground="White" FontFamily="Consolas"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </materialDesign:Card>

                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,20">
                                            <Button Content="Cancelar" Click="BackToSettingsList_Click" Style="{DynamicResource SecondaryButtonStyle}" Width="120" Margin="0,0,10,0"/>
                                            <Button Content="Salvar" Click="SaveMigrationsSettings_Click" 
                                                Style="{DynamicResource PrimaryButtonStyle}" Width="120"/>
                                        </StackPanel>
                                        </StackPanel>
                                    </ScrollViewer>
                                </Grid>

                                <!-- Painel: Configuração Ngrok -->
                                <Grid x:Name="NgrokSettingsPanel" Visibility="Collapsed">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                        <Button Content="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="16" 
                                            Foreground="White" Background="Transparent" BorderThickness="0"
                                            Click="BackToSettingsList_Click" Margin="0,0,10,0"/>
                                        <TextBlock Text="Configuração: Ngrok" FontSize="20" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                        <StackPanel Margin="0,0,20,0">
                                            
                                            <!-- Grupo: Execução Local -->
                                        <materialDesign:Card Padding="20" Margin="0,0,0,24" Background="#2D2D30">
                                            <StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                                    <materialDesign:PackIcon Kind="PlayCircle" Foreground="{DynamicResource AccentColor}" VerticalAlignment="Center" Margin="0,0,10,0" Width="20" Height="20"/>
                                                    <TextBlock Text="Execução Local" FontSize="16" Foreground="White" FontWeight="SemiBold"/>
                                                </StackPanel>

                                                <c:PathSelector x:Name="NgrokExeSelector" Title="Caminho do Executável do Ngrok (ngrok.exe)" IsFolderPicker="False"/>
                                            </StackPanel>
                                        </materialDesign:Card>

                                        <!-- Grupo: Autenticação e Argumentos -->
                                        <materialDesign:Card Padding="20" Margin="0,0,0,24" Background="#2D2D30">
                                            <StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                                    <materialDesign:PackIcon Kind="KeyVariant" Foreground="{DynamicResource AccentColor}" VerticalAlignment="Center" Margin="0,0,10,0" Width="20" Height="20"/>
                                                    <TextBlock Text="Autenticação e Argumentos" FontSize="16" Foreground="White" FontWeight="SemiBold"/>
                                                </StackPanel>

                                                <StackPanel>
                                                    <Label Content="Auth Token (Opcional - via --authtoken)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,6"/>
                                                    <TextBox x:Name="NgrokAuthTokenInput" Foreground="White" FontFamily="Consolas"/>
                                                </StackPanel>

                                                <StackPanel Margin="0,20,0,0">
                                                    <Label Content="Argumentos Adicionais" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,0,0,6"/>
                                                    <TextBox x:Name="NgrokArgsInput" Foreground="White" FontFamily="Consolas"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </materialDesign:Card>

                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,20">
                                            <Button Content="Cancelar" Click="BackToSettingsList_Click" Style="{DynamicResource SecondaryButtonStyle}" Width="120" Margin="0,0,10,0"/>
                                            <Button Content="Salvar" Click="SaveNgrokSettings_Click" 
                                                Style="{DynamicResource PrimaryButtonStyle}" Width="120"/>
                                        </StackPanel>
                                        </StackPanel>
                                    </ScrollViewer>
                                </Grid>
                            </Grid>
                        </TabItem>

                    </TabControl>
                </Grid>
            </Grid>
        </Border>
    </materialDesign:DialogHost>
</Window>