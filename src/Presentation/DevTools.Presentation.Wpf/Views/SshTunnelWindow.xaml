<Window x:Class="DevTools.Presentation.Wpf.Views.SshTunnelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:DevTools.Presentation.Wpf.Components"
        mc:Ignorable="d"
        Title="DevTools - SSH Tunnel"
        Height="650" Width="950"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        AllowsTransparency="True"
        Background="{StaticResource DevToolsWindowBackground}"
        BorderBrush="{StaticResource DevToolsBorder}"
        BorderThickness="1">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" ResizeBorderThickness="5" />
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource DevToolsSurface}" MouseLeftButtonDown="Header_MouseLeftButtonDown">
            <Grid Margin="12,0">
                <TextBlock Text="SSH Tunnel Manager" 
                           VerticalAlignment="Center" 
                           FontSize="{StaticResource DevToolsFontSize.SectionTitle}"
                           Foreground="{StaticResource DevToolsTextPrimary}"
                           FontWeight="SemiBold"/>
                
                <Button Content="✕" 
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Style="{StaticResource DevToolsWindowButtonStyle}"
                        Click="Cancel_Click"/>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="450"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Parameters -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,16,0">
                <StackPanel>
                    <c:ProfileSelector x:Name="ProfileSelector" ToolName="sshtunnel" Margin="0,0,0,16"/>

                    <StackPanel>
                        <TextBlock Text="Conexão Bastion (Jump Host)" FontSize="{StaticResource DevToolsFontSize.SectionTitle}" Foreground="{StaticResource DevToolsAccent}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,8">
                                <Label Content="Host SSH (IP ou Domínio)" Foreground="{StaticResource DevToolsTextSecondary}"/>
                                <TextBox x:Name="SshHostBox" Style="{StaticResource DevToolsTextInput}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,8">
                                <Label Content="Porta" Foreground="{StaticResource DevToolsTextSecondary}"/>
                                <TextBox x:Name="SshPortBox" Text="22" Style="{StaticResource DevToolsTextInput}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
                                <Label Content="Usuário SSH" Foreground="{StaticResource DevToolsTextSecondary}"/>
                                <TextBox x:Name="SshUserBox" Style="{StaticResource DevToolsTextInput}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <StackPanel Margin="0,16,0,0">
                        <TextBlock Text="Autenticação" FontSize="{StaticResource DevToolsFontSize.SectionTitle}" Foreground="{StaticResource DevToolsAccent}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <c:PathSelector x:Name="IdentityFileSelector" Title="Chave Privada (.pem, .ppk, id_rsa)" Margin="0,0,0,16"/>
                    </StackPanel>

                    <StackPanel Margin="0,16,0,0">
                        <TextBlock Text="Túnel (Port Forwarding)" FontSize="{StaticResource DevToolsFontSize.SectionTitle}" Foreground="{StaticResource DevToolsAccent}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,8">
                                <Label Content="Destino Remoto (Host na rede do Bastion)" Foreground="{StaticResource DevToolsTextSecondary}"/>
                                <TextBox x:Name="RemoteHostBox" Style="{StaticResource DevToolsTextInput}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,8">
                                <Label Content="Porta Rem." Foreground="{StaticResource DevToolsTextSecondary}"/>
                                <TextBox x:Name="RemotePortBox" Style="{StaticResource DevToolsTextInput}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,0">
                                <Label Content="Bind Local (ex: 127.0.0.1)" Foreground="{StaticResource DevToolsTextSecondary}"/>
                                <TextBox x:Name="LocalBindBox" Text="127.0.0.1" Style="{StaticResource DevToolsTextInput}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="1">
                                <Label Content="Porta Local" Foreground="{StaticResource DevToolsTextSecondary}"/>
                                <TextBox x:Name="LocalPortBox" Style="{StaticResource DevToolsTextInput}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                    
                    <StackPanel Margin="0,16,0,0">
                        <TextBlock Text="Opções" FontSize="{StaticResource DevToolsFontSize.SectionTitle}" Foreground="{StaticResource DevToolsAccent}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <CheckBox x:Name="CompressionCheck" Content="Compressão (-C)" Margin="0,0,16,0" Style="{StaticResource DevToolsCheckBox}"/>
                            <CheckBox x:Name="VerboseCheck" Content="Verbose (-v)" Style="{StaticResource DevToolsCheckBox}"/>
                        </StackPanel>
                    </StackPanel>

                    <Button x:Name="ToggleTunnelButton" Content="Conectar Túnel" Click="ToggleTunnel_Click" 
                            Margin="0,24,0,0" Height="40" 
                            Style="{StaticResource DevToolsPrimaryButton}"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Status/Logs -->
            <Border Grid.Column="1" Background="{StaticResource DevToolsSurface}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="Status: " FontWeight="Bold" Foreground="{StaticResource DevToolsTextPrimary}"/>
                        <TextBlock x:Name="StatusText" Text="Desconectado" Foreground="{StaticResource DevToolsTextSecondary}"/>
                    </StackPanel>
                    
                    <TextBox Grid.Row="1" x:Name="LogBox" 
                             IsReadOnly="True" 
                             TextWrapping="Wrap" 
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="{StaticResource DevToolsTextPrimary}"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
